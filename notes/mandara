When it's not possible to chsh to z-shell (b/c of LDAP permissions):
Add:
~~~
# Run ZSH
if [ -z "${BASH_EXECUTION_STRING}" ]; then
    ZSH="/bin/zsh"
    [ -x "${ZSH}" ] && SHELL="${ZSH}" exec "${ZSH}" -l
fi
~~~
to ~/.bash_profile
